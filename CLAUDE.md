# Project Context

Obsidian notes: `~/Desktop/Vault/Work/Projects/fsisim-poc-scaffold/Project.md`
Implementation plan: `~/Desktop/Vault/Work/Projects/fsisim-poc-scaffold/Implementation-Plan.md`

Account: FlightSafety (Chad's account)
Parent spec: `FSI SIM POC Solution Overview - May 14.docx` (Matt Geissman's recommendation)
Workspace: DEFAULT (Jonathan's personal Databricks workspace)
Catalog: `jdub_demo` (configurable in `config.py`)

This is a *scaffold* build with mock data, not a customer-facing POC. Matt's team will fork
and repoint catalog to `poc_data` when they take it over.

Style:
- No em or en dashes. Use colons, semicolons, parens.
- Single config file (`config.py`) is the source of truth for catalog/schema/index names.
